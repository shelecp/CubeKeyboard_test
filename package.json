{
  "name": "cube-keyboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "魔方键盘：3D 魔方展示与可编程扭动-字符映射原型",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/test-model.mjs && node scripts/test-rules.mjs && node scripts/test-gyro.mjs && node scripts/test-camera.mjs && node scripts/test-orientation.mjs && node scripts/test-ime.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
